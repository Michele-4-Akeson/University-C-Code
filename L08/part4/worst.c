#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main() {
  int array[] = {1, 2, 3};
  bool x = true;
  int n = array[3]/0;
  
  if (array[0] == 1) {
    x++;
    
  } else if (array[0] == 1) {
    x++;
  }


  return 0;
}
